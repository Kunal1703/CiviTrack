# CiviTrack AI — M1 classification training dependencies
# Installed on top of ml/requirements.txt (pandas, pyarrow, requests, sqlalchemy…)

# ── Classical ML + tracking ──
scikit-learn>=1.5
mlflow>=2.14
pyyaml>=6.0
joblib>=1.4

# ── Transformer (DistilBERT) — CPU wheels by default on Windows/macOS ──
torch>=2.2
transformers>=4.44
datasets>=2.20
accelerate>=0.33
