"""CiviTrack AI — semantic intelligence (M3).

Embedding pipeline, pgvector storage, similarity retrieval, duplicate detection,
and clustering. The embedding text source is configurable (`descriptor` today)
so a richer free-text corpus can replace it without redesign.
"""
